---
title: Implementation and tests
description: A developer index showing which implementation and tests maintain Tobari public security behavior.
---

This page is a developer index for tracing Tobari's public security behavior into the repository. It maps important behavior to the relevant design documents, implementation, and tests.

Use it to inspect topology, principal, policy, TLS, credential, lifecycle, and
supply-chain implementation at the commit fixed by this documentation. Read
[Supply chain](../supply-chain/) and the relevant How it works page before
interpreting an individual evidence row.

## Public behavior, implementation, and tests

| Claim                                                                         | Enforcement                                                                  | Tests                                                                            | Governing document               | Relevant source                              |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------- | -------------------------------------------- |
| Workspace has no supported direct egress                                      | Dedicated Docker `internal` project network; only Gateway joins egress       | `scripts/test-integration.sh` direct-egress and transparent-routing cases        | `docs/03_security_model.md`      | `compose.yaml`, `project_runtime.go`         |
| Workspace does not receive Docker control or a route to OPA/another Workspace | Mount/network topology and per-project network                               | Integration direct-OPA, Docker socket, cross-Workspace cases                     | Security model                   | `compose.yaml`                               |
| Project identity is not request self-assertion                                | Kernel-observed Workspace source lookup in host schema-1 principal registry  | `principal_registry_test.go`; forged-header and unknown-principal Gateway tests  | ADR 0011, architecture           | `principal_registry.go`, `tobari_gateway.py` |
| OPA failure is closed                                                         | One fixed body-free query; Gateway requires valid structured allow           | Gateway timeout/non-200/malformed tests; OPA outage integration                  | External API contracts           | `tobari_gateway.py`                          |
| Deny creates no upstream connection                                           | Lazy upstream connect after policy allow                                     | Gateway denied-body/non-forward tests; integration denial target                 | Security model                   | `tobari_gateway.py`                          |
| HTTPS is intercepted and re-established                                       | Transparent mitmproxy/Tobari CA termination plus verified upstream TLS       | Gateway transparent-authority tests; CA/intercept integration                    | ADR 0026, architecture           | `gateway/entrypoint.sh`, runtime entrypoint  |
| Request body is not rule identity                                             | OPA input builder excludes body; policy candidate grouping excludes body     | Gateway body-independent input; integration body-grouping and streaming canaries | External API contracts           | Gateway addon, policy domain                 |
| Denial cannot self-approve                                                    | Host-only opaque reference action and complete policy activation             | service/policydata/aggregate tests; review/allow/deny integration                | Product/security contracts       | policy app/infra implementation              |
| Learned rules are Workspace Manifest/project exact                            | Rego/data rule fields and aggregate projection                               | Rego tests; Workspace Manifest/project separation integration                    | Architecture/security model      | `tobari.rego`, `policydata.go`               |
| Broker credential state stays outside Workspace                               | Encrypted Workspace Manifest vault and handle-only projection                | Broker vault tests; project auth tests; static-secret absence canaries           | Authentication/security contract | `vault.py`, `project_auth.go`                |
| GitHub acquisition remains on the trusted host                                | Fixed identity-checked `gh` helper with sanitized temporary home and cleanup | `credentialhost` helper/state tests; host-login integration fixtures             | Authentication contract          | `internal/infra/credentialhost`              |
| OPA deny performs no secret-bearing broker action                             | Introspect before policy; same-revision static resolve only after allow      | Gateway ordered-call tests prove zero resolution on deny                         | Authentication contract          | `broker_credentials.py`, `broker.py`         |
| Invalid Tobari handle never falls back                                        | Marker-recognition path commits to fail-closed broker processing             | Malformed/stale/copied/binding mismatch Gateway/Broker tests                     | Authentication/security contract | `broker_credentials.py`, `broker.py`         |
| Root key/vault unsafe state fails closed                                      | Owner-only/symlink checks, authenticated encryption, missing-key guard       | `test_broker.py`, rootkey/dockerruntime tests                                    | Authentication/security contract | `rootkey`, `vault.py`                        |
| `exit`, `delete`, `down`, `purge` have different ownership effects            | App lifecycle plus project/cluster state stores                              | service/project runtime/state tests and integration lifecycle                    | Product/architecture             | `service.go`, project runtime/state          |
| Runtime CMD does not own Workspace lifetime                                   | Tobari sets fixed `sleep infinity` and API label                             | runtime tests and runtimecheck                                                   | Architecture/runtime contract    | `project_runtime.go`, runtime metadata       |
| Embedded source cannot silently drift                                         | Canonical/snapshot byte/content checks                                       | source-check scripts in `task check`                                             | Harness/release                  | runtime assets and sync/check scripts        |
| Service images have exact identities                                          | `versions.env` digest pins and image verification                            | runtime assets/Gateway/Broker image tests                                        | Security/release                 | `versions.env`                               |

All links below are fixed to the source commit documented by this site.

## Direct evidence links

### Governing documents

- [Architecture](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/docs/02_architecture.md)
- [Security model](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/docs/03_security_model.md)
- [Harness](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/docs/04_harness.md)
- [Authentication](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/docs/07_authentication.md)
- [External API contracts](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/docs/08_external_api_contracts.md)

### Enforcement and tests

- [Gateway implementation](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/gateway/addon/tobari_gateway.py) and [tests](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/gateway/test_tobari_gateway.py)
- [OPA policy](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/runtimeassets/assets/opa/policy/tobari.rego) and [tests](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/runtimeassets/assets/opa/policy/tobari_test.rego)
- [Auth Broker](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/authbroker/broker.py), [vault](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/authbroker/vault.py), and [tests](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/authbroker/tests/test_broker.py)
- [Trusted-host drivers](https://github.com/tasuku43/tobari/tree/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/credentialhost) and [host-login tests](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/dockerruntime/auth_host_login_test.go)
- [Project principal registry](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/dockerruntime/principal_registry.go) and [tests](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/internal/infra/dockerruntime/principal_registry_test.go)
- [End-to-end integration suite](https://github.com/tasuku43/tobari/blob/2a0f0f3b0303f889cd42c581403e70ace7680894/scripts/test-integration.sh)

## How to interpret gaps

A unit test can prove a parser or state invariant without proving the real Docker topology. An integration test can prove an observed topology without proving every invalid document is rejected. Architecture plus configuration can explain the two-TLS design even though the current suite does not packet-capture both handshakes. This map therefore names different evidence types instead of pretending one test proves the entire claim.

<aside class="limitation">
  <h2>Evidence limitation</h2>
  <div class="prose-block">
    Some preservation behavior—such as vault/root-key survival across cluster
    purge—is visible in deletion ownership and implementation, but does not have
    a dedicated single assertion for every retained file. The table
    distinguishes implementation evidence from direct test coverage rather than
    overstating it.
  </div>
</aside>

## You should now be able to explain…

…which test or source you would inspect when a claim changes.

<div class="question-list">
  <details>
    <summary>Which evidence proves a forged project header is ignored?</summary>
    <div class="prose-block">
      Gateway principal tests plus `principal_registry.go` and ADR 0011. The
      kernel-observed Workspace source endpoint, not the header, supplies
      authority.
    </div>
  </details>
  <details>
    <summary>
      Which evidence proves an OPA outage does not connect upstream?
    </summary>
    <div class="prose-block">
      Gateway policy-failure tests and the OPA-outage integration case,
      interpreted with the lazy-connection implementation.
    </div>
  </details>
  <details>
    <summary>Where do you verify exact CLI fault recovery?</summary>
    <div class="prose-block">
      The generated{" "}
      <a href="../../reference/faults-and-recovery/">Faults and recovery</a>{" "}
      page derives it from Catalog agent help.
    </div>
  </details>
</div>

<div class="page-next">
  <strong>Continue:</strong> use the symptom-first{" "}
  <a href="../../guides/troubleshooting/">Troubleshooting guide</a>, or inspect
  the generated <a href="../../reference/cli/">CLI reference →</a>
</div>
