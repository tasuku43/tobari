---
title: credential
description: Workspace ネイティブ認証と post-policy credential 境界を説明します。
---

## standard の所有権

agent CLI はネイティブ認証状態を一つの Workspace の永続 home 内に作成・保存
します。Tobari は host credential を継承・コピーせず、standard profile には
provider binding も Auth Broker もありません。

同じ Workspace 内の全プロセスはその状態を読めます。別 Workspace は別の home
を持ちます。Workspace を削除すると tool-owned state も削除されます。

## post-policy forwarding

Gateway は認証情報を policy identity ではなく secret request data として扱います。
Authorization、API key、cookie を OPA input と audit から除外し、通常の exact HTTP
effect を OPA に問い合わせ、allow 後だけ元の値を渡します。deny では DNS も
upstream connection も実行しません。

したがって login と permission は独立しています。

- native login は policy rule を追加しません。
- allow は credential の作成や refresh をしません。
- policy が許可すれば same-Workspace process は認証済み effect を利用できます。
- Tobari は `claude`、`codex` などの process name から権限を推測しません。

## agent-ready の login effect

pinned agent-ready compatibility baseline は Claude の hello/token/profile/roles と
Codex の browser/device token effect を exact に含みます。Claude regression は native token exchange 後も
subscription と rate-limit classification を含む provider profile metadata を保持
します。

## experimental Broker

repository は `task build:dev` の背後に experimental Broker を保持します。handle、
vault、root key、provider plan、3-service topology は、standard command、image、state
projection、release lock、publication のいずれにも現れません。

## security limit

Workspace ネイティブ認証は host と別 Workspace home を保護しますが、同じ
Workspace 内での process-level secret isolation ではありません。認証済み request
の送信先は exact network policy が制御します。
