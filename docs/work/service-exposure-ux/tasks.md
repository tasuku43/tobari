# Work Tasks: Shorten reviewed Workspace service exposure

- Goal: [goal.md](goal.md)
- Plan: [plan.md](plan.md)

This is the authorized implementation checklist. Production implementation is
started but is not complete in this packet.

- Design status: Accepted/Fixed
- Implementation status: In progress from exact accepted HEAD `dd1af498`

## Mandatory implementation-entry gate

- [x] Observe completion in the fixed order: WP01 + WP02 audit -> WP08 -> WP03
      -> WP04 -> WP07 -> WP11 -> WP05 -> WP09. Record each completion handoff and do not
      partially reproduce an upstream mechanism. WP06 waits for the WP09
      summary handoff.
- [x] After the final upstream handoff, create an independent clean worktree;
      record exact `HEAD`, upstream and
      nominated integration commit, branch, and complete `git status`; preserve
      every unrelated change and stop if overlapping Service/identity/schema/
      Catalog/helper files remain unowned or actively changing.
- [x] Re-read `AGENTS.md`, docs 00--04 in numeric order, ADR 0074 and ADRs
      0081--0084,
      `07535a9`/`428812f` promotion evidence, and the actual integrated
      Context/Workspace/Template presentation identity, clean-break isolation, WP08
      recursive traversal, WP03 Runtime protection, WP04 research boundary,
      WP05 opposite-direction hostname/name, WP07 permission wait, attachment/
      registry, Service, schema/JSON/status, helper closure, and tests.
- [x] Verify exact clean accepted baseline `dd1af498`, branch
      `codex/wp09-service-exposure-ux`, and ancestry of WP03 `922fa792`, WP04
      `cc5d14b`, WP07 `77c5607`, and WP11 corrective `6ccdb424` before edits.
- [x] Fix the consumed seams: ContextID + WorkspaceID authority, Template-derived
      presentation only, canonical attachment identity/liveness only, and a
      distinct Service owner branch with no Permission Inbox, Policy Memory,
      Host Loopback grant/route, Template copy/inheritance, or research state.
- [ ] Rerun current help and empty Service review/status observations with fresh
      temporary XDG roots and only bounded read-only diagnostics. Record exact
      commit, output/schema, and zero-write evidence; create no listener,
      attachment, Docker state, or browser effect to satisfy this gate.
- [ ] Compare the landed contract with this packet's authority tuple,
      task partition, reference flow, bounded observation, origin/Host, browser,
      cleanup, migration, and schema contracts. Report `WP09_BLOCKED` rather
      than choose a local fallback if they conflict.
- [ ] Assign one owner and sequence for each overlapping production/schema/
      Catalog/helper/public-guard file. Do not pre-implement any upstream
      mechanism from this Service slice.

## Understand

- [ ] Treat every post-gate upstream identity, activation, recursive-reference,
      Runtime-protection, build-profile, Host Loopback, permission-wait, and
      migration interface as consumed rather than reopened here.
- [ ] Re-run the current helper/review/status/exit observations and record the
      exact source commit, platform, outputs, interaction count, and zero-write
      fresh-XDG result.
- [ ] Re-run the existing synthetic HTTP/WebSocket and representative
      development-server compatibility observations without live credentials
      or private project content.
- [ ] Confirm current attachment owner/borrower behavior and whether each
      session owns a distinct Service controller under concurrency.
- [ ] Record the final child-session AttachmentID/epoch/controller interface and
      prove exposure authority is never inherited by Manifest name/revision,
      Workspace label, root, Runtime, port, URL, or proximity.
- [ ] Record owner-snapshot races under one fixed anchor and implement the fixed
      complete/partial/unavailable plus owner-count semantics without cleanup.
- [ ] Confirm the public outcome, non-goals, concept count, and complete absence
      of WP05 Host Loopback naming/hostname from Service output.
- [ ] Re-read ADR 0079/docs 01--02 for promoted WP02 semantics and the
      Product-Owner-fixed WP03 packet; record only their Service boundaries:
      copy transfers no attachment/Service/observed state and causes no
      reconciliation; Runtime lifecycle never cascades Service cleanup and
      instead fails closed on live Workspace/container protection.

## Product Owner decisions fixed

- [x] Accept pending-only `review services --watch`, one raw action key or one
      full line token as confirmation, and no second yes or direct-action
      confirm flag.
- [x] Keep one `service-request` selection kind; active exposure discovery and
      Open/Stop start from `service status`; add no V1 typed multi-selection.
- [x] Fix redirected/JSON review as read-only and gate watch/notify to trusted
      interactive text TTY before any read/action with evidence-free cues.
- [x] Replace host `service requests` and helper `list` with separate status
      tasks and no aliases; keep CWD status to ref/URL/port/action-free summary.
- [x] Fix complete delivery, `bounded_window`, one registry anchor,
      complete/partial/unavailable states, bounded owner counts, fail-closed
      ambiguity, and zero cleanup from reads.
- [x] Fix the exposure locator structurally as `scheme = http`, generated exact
      `.localhost` authority, and `path = /`; keep `tcp4 127.0.0.1:0`, exact Host
      validation/forwarding, no numeric fallback, and cookie isolation gate.
- [x] Fix access authority versus lifecycle-ref separation and Allow-before-Open
      ordering with the three typed Open outcomes.
- [x] Fix listener-first bounded teardown, count-only receipt, no durable
      migration, atomic host/helper cutover, and WP01 zero-live-attachment gate.
- [x] Fix cross-packet isolation and implementation order; WP08 alone owns
      recursive reference traversal and WP06 consumes only the later summary.
- [x] Require ADR 0074 and governing thesis/product/architecture/security/
      harness promotion before mechanism and the named completion gates/handoff.

## Freeze semantic evidence

- [ ] Add one presentation-independent typed fixture containing known-empty,
      one/many pending, stale replacement, active, relaying, target-unavailable,
      anchored complete/partial/unavailable owner counts, generated origins,
      Open outcomes, and cleanup receipt cases.
- [ ] Add a machine-readable answer key with every task/scope dimension,
      canonical ref, exact next argv, and absent/empty/zero/false/unavailable
      distinction.
- [ ] Add same-label, same-port, same-root, adjacency, order, indentation, and
      quoted-prose negative-inference canaries.
- [ ] Add hostile Host/absolute-form/framing and supported-browser cookie-origin
      fixtures, including sibling generated hosts and `Domain=localhost`.
- [ ] Record current and proposed human goldens, hashes, bytes, interaction
      counts, and external-processing counts using the same fixture.
- [ ] Record the human-handoff scorecard for current, watch, merged inbox,
      direct host, terminal, clipboard, and browser alternatives.

## Contract and domain

- [ ] Define separate pending-only review, host status, helper status, and CWD
      summary types; fixed-anchor observation/counts; request/exposure rows;
      access locator; Open outcomes; and cleanup receipt.
- [ ] Bind request/exposure scope to
      WorkspaceManifestID + WorkspaceID + AttachmentID/epoch + trusted
      principal/controller + exact target + exact request/exposure identity and
      prove Manifest revision/name, Runtime, ProjectRoot, row, ports, URL, and
      order cannot substitute for it.
- [ ] Prove a recommended Manifest draft, absent Manifest catalog, detached or
      absent Workspace, and missing attachment epoch make zero Service calls
      and produce no request/exposure ref.
- [ ] Keep Service request/exposure/cleanup types outside Workspace Manifest
      desired revisions, Workspace AppliedEntry, and bounded reconciliation
      failure types; preserve the three-durable-resource concept budget.
- [ ] Preserve exact request/exposure validation and public/internal vocabulary
      separation.
- [ ] Define access authority separately from lifecycle-ref authority and define
      the structural locator (`scheme = http`, generated authority, `path = /`)
      plus `open_not_dispatched|open_requested|open_outcome_unknown` without
      socket, process, DNS, or browser types in domain.
- [ ] Update Catalog command specs, formats, recursive output fields, faults,
      next actions, roles, effects, impacts, and reference bindings.
- [ ] Consume the Catalog-wide recursive nested output-reference derivation and
      add generic/Service fixtures; add no Service-specific producer walker,
      reachability validator, or second schema checker.
- [ ] Prove global Program-aware reference closure for request Allow/Deny and
      exposure Open/Stop across both executables.
- [ ] Prove `review services` and `service status` produce request refs;
      `service status`, helper create/status, and Allow produce exposure refs;
      Allow consumes one request parent; Deny/Open/Stop consume exact refs.
- [ ] Add negative Catalog tests for optional, wrong-kind, duplicate, extra,
      hidden-renderer, or reconstructed bindings, a second review selection
      kind, and typed multi-selection groups.
- [ ] Remove host `service requests` and helper `list` from routing, help,
      completion, recovery, fixtures, and dormant handlers; add no aliases.

## Application

- [ ] Add task-owned read ports for current-attachment and host-wide Service
      snapshots with a fixed bounded registry anchor, complete/partial/
      unavailable observation, and bounded owner counts.
- [ ] Add host exposure Stop and purpose-limited Open use cases through the
      canonical mutation invoker.
- [ ] Validate request/exposure task identity, WorkspaceManifestID,
      WorkspaceID, AttachmentID/epoch, principal/controller, target, and
      reference kind before adapter calls; exact actions freshly re-resolve and
      revalidate owner/UID/PID/nonce/attachment/ref.
- [ ] Preserve confirmed Allow before optional Open and prohibit Allow replay
      recovery after opener failure.
- [ ] Add cleanup receipt ordering and result validation.
- [ ] Add fake-port tests for zero-call invalid inputs, anchored snapshot races,
      unsafe/duplicate/contradictory owners, Allow/Open ordering, exact Stop,
      cancellation phases, and all three Open outcomes.
- [ ] Add zero-call tests proving Manifest revision publication, adoption
      pending, attached-blocked entry, status reads, and reconciliation failure
      observation neither stop nor recreate Service state.
- [ ] Add zero-call tests proving Manifest copy and Runtime source copy do not
      create, clone, resolve, or reconcile an attachment/request/exposure and
      cannot select one through source name, provenance, or copied content.
- [ ] Add integration-port tests proving Runtime build/restore/delete/prune
      never call Service Stop or attachment cleanup, preserve existing exposure
      identity, and expose live Workspace/container state only as a fail-closed
      WP03 protection result.

## Infrastructure

- [ ] Extend the private owner snapshot atomically to include active exposures,
      one registry anchor, observation state/counts, and exact owner identity
      without a generic method bus or cleanup during reads.
- [ ] Atomically replace predecessor Context/project/instance authority fields
      with WorkspaceManifestID/WorkspaceID/AttachmentID-or-epoch/trusted
      principal-controller after WP01; accept no fallback or normalized legacy
      record.
- [ ] Preserve owner-only regular records, unpredictable socket/nonce, peer
      PID/UID, attachment checks, limits, and timeouts. Move stale cleanup to
      its separate owner; discovery reads never perform it.
- [ ] Add host exposure Stop and active exposure resolution by opaque ref.
- [ ] Add a Service-specific browser adapter that accepts only the
      owner-derived validated confirmed exposure URL and fixed platform argv;
      accept no caller locator component or browser argv.
- [ ] Prove the browser-login socket/schema/registry and Service opener remain
      separate.
- [ ] Classify `open_not_dispatched`, `open_requested`, and
      `open_outcome_unknown` on macOS and Linux with finite timeout and
      sanitized environment tests; never claim browser load.
- [ ] Generate a fresh independent 128-bit lowercase DNS label per exposure,
      bind only `tcp4 127.0.0.1:0`, and persist no derivation between label and
      exposure ref outside the live owner.
- [ ] Require exact generated Host hostname/assigned port and matching absolute-
      form authority before Workspace I/O; reject numeric, bare localhost,
      sibling, wrong-port, missing/duplicate/malformed, and rebinding values.
- [ ] Forward the accepted Host and ordinary headers/cookies/content unchanged;
      preserve framing, WebSocket `101`, 502, backpressure, and limits.
- [ ] Close listener admission first, terminate/drain relays within the fixed
      bound, confirm closure, then remove owner state and return bounded count-
      only cleanup evidence.
- [ ] Sync the dedicated helper source closure and embedded runtime asset using
      repository scripts; do not edit the generated snapshot independently.

## CLI and presentation

- [ ] Extract/extend the task-neutral review presentation primitive with raw
      mode, safe-key detection, full-line fallback, alternate screen, stable-ID
      reconciliation, watch/backoff, fixed cue, stale disablement, semantic
      styles, and restoration.
- [ ] Keep Permission Inbox staging/Apply and Service immediate actions in
      separate task adapters and typed models.
- [ ] Implement single-request direct card and multiple-request selection with
- [ ] Accept only raw `a|o|d|b` or line `allow|open|deny|back`; add no second yes
      prompt and no direct-action confirmation flag.
- [ ] Implement `review services --watch` and `--notify`; reject JSON/redirected
      or non-trusted/non-text/non-interactive use before reads or actions, and
      keep cue payloads free of root, port, URL, ref, ID, or external text.
- [ ] Implement explicit Allow-and-open as confirmed Allow followed by the
      canonical reference-bound open action.
- [ ] Implement pending-only `review services`, host `service status`, helper
      `status`, host Open/Stop, and CWD `status [--manifest NAME]` summary from
      the same validated facts without cleanup or repair. CWD emits no Service
      ref, URL, port, or action.
- [ ] Implement distinct schema-1 review/host-status/helper-status/CWD-summary
      envelopes with exact empty/partial/unavailable semantics and ensure
      pending guidance never corrupts helper stdout JSON.
- [ ] Render access wireframes symbolically as “confirmed exposure URL”; stable
      contracts describe scheme, authority, and path separately rather than
      embedding a full private-network-looking locator in packet evidence.
- [ ] Implement count-only session-close cleanup receipt presentation with no
      ID, URL, port, secret, or stale authority after teardown.
- [ ] Add hostile-output, short-write, late-cancellation, raw/line fallback,
      repaint, cue, and terminal-restoration tests.

## Compatibility, migration, and docs

- [ ] Prove there is no durable Service state migration and incompatible live
      records fail closed until attachment re-entry.
- [ ] Prove predecessor Context UUID bytes and ProjectInstance UUID bytes may
      become WorkspaceManifestID and WorkspaceID only through ADR 0079's
      durable migration; request/exposure/listener/socket/attachment bytes are
      never migration input or output.
- [ ] Enforce WP01's zero-live-attachment migration precondition, ordinary
      attachment teardown, atomic private host/helper protocol cutover, and
      exact exit/re-entry recovery. Migration never silently stops or translates
      an exposure.
- [ ] Prove listener, exposure URL, owner record, and target reachability are
      never accepted as ADR 0079 migration evidence for applied state.
- [ ] Prove Service URL, host port, connection count, relay traffic, and Service
      timestamps are never accepted as Runtime `last_used`, prune eligibility,
      retirement authority, copy provenance, or restore/build selection.
- [ ] Prove `manifest create --copy-from` and
      `runtime create --copy-source-from` leave source Service state unchanged,
      create no target Service state, and persist/display no Service lineage;
      do not add a `--base` compatibility path.
- [ ] Prove no host `service requests` or helper `list` alias, old public field,
      compatibility reader, hidden browser opener, direct host expose, numeric-
      URL fallback, or broad Host acceptance remains reachable.
- [ ] Add negative target-V1 vocabulary tests for public and semantic
      `Context`, `context_id`, `--context`, `project_id`, `instance_id`, and
      `ProjectInstance` across Service Catalog, JSON, protocols, help,
      recovery, fixtures, and generated artifacts. Current-main migration
      decoder evidence remains explicitly predecessor-only.
- [ ] Update theses, product, architecture, security, harness, ADR 0074,
      README, agent readiness, public/release contracts, capability/schema
      ledgers, Skill, and architecture site in the same change.
- [ ] Update the public guard through its governing contract with a narrow exact
      matcher only for the generated Service authority: fixed `svc-` prefix,
      exact lowercase random-label grammar/length, exact `.localhost`, and
      validated port/locator structure where applicable. Do not permit arbitrary
      subdomains, bare localhost, `.local`, other private hosts, caller URLs, or
      a broad wildcard/suffix.
- [ ] Add public-guard positive/negative tests proving the narrow generated-
      authority exception passes valid structural documentation/output but
      cannot mask arbitrary private-network text.
- [ ] Update English/Japanese site routes and generated snapshot/evidence under
      their existing locale and provenance contracts.
- [ ] Update helper source/public/release license and integrity evidence without
      adding an archive binary or new dependency.

## Verify

- [ ] Focused domain/application/CLI/Catalog tests pass. Evidence:
- [ ] Rendezvous/relay/browser/asset tests pass on supported host fixtures. Evidence:
- [ ] Supported-browser tests prove cookie isolation between two generated
      exposure hosts and no sibling contamination from `Domain=localhost`; a
      contradiction reports `WP09_BLOCKED`. Evidence:
- [ ] Engine/Desktop/Colima readiness evidence passes with exact versions and
      bounded synthetic fixtures. Evidence:
- [ ] Clean Docker integration covers request, watch discovery, Allow, optional
      Open, exact HTTP/WebSocket, helper/host status, host/helper Stop, and
      attachment cleanup. Evidence:
- [ ] ADR 0079 integration covers Manifest revision publication,
      attached-blocked adoption with an unchanged exposure, observed-only CWD
      status, explicit attachment drain, durable migration, `cluster up`,
      re-entry, and a new non-migrated exposure. Evidence:
- [ ] Promoted copy-isolation integration covers fresh Manifest/Runtime IDs, zero
      copied Service/attachment/observed state, zero reconciliation, and an
      unchanged source exposure. Evidence:
- [ ] WP03 integration covers non-cascading Runtime lifecycle, fail-closed live
      Workspace/container protection, unchanged exposure/attachment authority,
      and unknown `last_used` absent exact separately approved evidence.
      Evidence:
- [ ] `task check` passes. Evidence:
- [ ] `task security` passes. Evidence:
- [ ] `task public:check` passes. Evidence:
- [ ] `task release:check` passes. Evidence:
- [ ] Current public guard passes with the narrow generated-authority exception
      and its positive/negative tests; unrelated private-network strings remain
      rejected. Evidence:
- [ ] The agent-readiness journey meets one helper plus one host task (or
      pre-running watch), one explicit action, zero guesses, zero identifier
      reconstruction, and zero external processing. Evidence:
- [ ] Presentation fixture/golden hashes and answer-key validation pass. Evidence:
- [ ] Generated diffs and final repository status are understood. Evidence:

## Hand off

- [ ] Every acceptance criterion has evidence and no unresolved cross-cutting
      review blocks the chosen public contract.
- [ ] Durable decisions are promoted out of this packet before implementation
      is declared complete.
- [ ] Temporary diagnostics, browser history, packet-only goldens, and any
      sensitive artifacts are removed.
- [ ] Goal status changes to `Complete` only after every criterion/task is
      complete, then this temporary packet is removed from the final tree.
- [ ] Handoff summarizes outcome, interaction delta, public concepts,
      retired/renamed surfaces, JSON/migration, trust boundary, packet
      dependencies/conflicts, confirms the ADR 0079 authority cutover,
      checks, and remaining risks.
- [ ] After commit and packet removal, notify the control thread with
      `WP09_IMPLEMENTATION_COMPLETE` or `WP09_BLOCKED`, final interfaces, gate
      evidence, HEAD/status, retention, and whether WP06 may consume the
      summary.
