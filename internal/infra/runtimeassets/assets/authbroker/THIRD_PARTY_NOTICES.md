# Auth Broker third-party notices

The Auth Broker image includes GitHub CLI 2.96.0, distributed by GitHub under
the MIT License. The binary is downloaded only from the corresponding upstream
GitHub CLI release and verified against the pinned SHA-256 values in
`gh-checksums.txt` for Linux amd64 and arm64.

Upstream source and license: <https://github.com/cli/cli/tree/v2.96.0>

