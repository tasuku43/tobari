diff --git a/docs/architecture-site/src/components/HomeExperience.astro b/docs/architecture-site/src/components/HomeExperience.astro
--- a/docs/architecture-site/src/components/HomeExperience.astro
+++ b/docs/architecture-site/src/components/HomeExperience.astro
@@ -1,4 +1,10 @@
 ---
+interface Props {
+  heading?: string;
+  lead?: string;
+  caveat?: string;
+}
+
 const isJapanese = Astro.locals.starlightRoute.locale === "ja";
 const t = <T,>(english: T, japanese: T) => (isJapanese ? japanese : english);
 const withBase = (path: string) =>
@@ -125,6 +131,25 @@ const evidence = [
     "scripts/test-integration.sh",
   ],
 ] as const;
+
+const {
+  heading = t(
+    "Run a coding agent inside one selected project.",
+    "コーディングエージェントを、選んだプロジェクトの中で動かす。",
+  ),
+  lead = t(
+    "Outbound HTTP and HTTPS communication starts denied. Review what the agent attempted, allow only the exact communication you need, and then issue a new request.",
+    "外部への HTTP/HTTPS 通信は、最初に止めます。内容を確認し、必要な通信だけを完全一致で許可してから、もう一度実行します。",
+  ),
+  caveat = t(
+    "Important: files inside the selected project are not protected from the agent. The agent can read and write that mounted project.",
+    "重要: 選んだプロジェクト内のファイルは、Tobari の保護対象ではありません。エージェントはその範囲を読み書きできます。",
+  ),
+} = Astro.props;
 ---
 
 <div class="home-experience">
@@ -143,23 +168,8 @@ const evidence = [
           )
         }
       </p>
-      <h2>
-        {
-          t(
-            "Run a coding agent inside one selected project.",
-            "コーディングエージェントを、選んだプロジェクトの中で動かす。",
-          )
-        }
-      </h2>
-      <p class="home-lead">
-        {
-          t(
-            "Outbound HTTP and HTTPS communication starts denied. Review what the agent attempted, allow only the exact communication you need, and then issue a new request.",
-            "外部への HTTP/HTTPS 通信は、最初に止めます。内容を確認し、必要な通信だけを完全一致で許可してから、もう一度実行します。",
-          )
-        }
-      </p>
+      <h2>{heading}</h2>
+      <p class="home-lead">{lead}</p>
       <div class="home-actions">
         <a class="primary" href={withBase("/start/quickstart/")}>
           {t("Try it in five minutes", "5分で試す")}
@@ -168,14 +178,7 @@ const evidence = [
           {t("See how it works in 60 seconds", "60秒で仕組みを見る")}
         </a>
       </div>
-      <p class="home-caveat">
-        {
-          t(
-            "Important: files inside the selected project are not protected from the agent. The agent can read and write that mounted project.",
-            "重要: 選んだプロジェクト内のファイルは、Tobari の保護対象ではありません。エージェントはその範囲を読み書きできます。",
-          )
-        }
-      </p>
+      <p class="home-caveat">{caveat}</p>
     </div>
 
     <aside
diff --git a/docs/architecture-site/src/components/QuickstartWalkthrough.astro b/docs/architecture-site/src/components/QuickstartWalkthrough.astro
--- a/docs/architecture-site/src/components/QuickstartWalkthrough.astro
+++ b/docs/architecture-site/src/components/QuickstartWalkthrough.astro
@@ -1,4 +1,10 @@
 ---
+interface Props {
+  intro?: string;
+  prerequisite?: string;
+  completion?: string;
+}
+
 const isJapanese = Astro.locals.starlightRoute.locale === "ja";
 const withBase = (path: string) =>
   `${import.meta.env.BASE_URL}${isJapanese ? "ja/" : ""}${path.replace(/^\//, "")}`;
@@ -236,12 +242,18 @@ tobari policy allow --id <exact-pcy-id-from-policy-review>`,
         ["End-to-end integration checks", "scripts/test-integration.sh"],
       ],
     };
+
+const {
+  intro = ui.intro,
+  prerequisite = ui.prerequisite,
+  completion = ui.completion,
+} = Astro.props;
 ---
 
 <section class="quickstart-walkthrough">
-  <p class="quickstart-intro">{ui.intro}</p>
+  <p class="quickstart-intro">{intro}</p>
   <aside class="quickstart-prerequisite">
-    <p>{ui.prerequisite}</p>
+    <p>{prerequisite}</p>
     <a href={withBase("/start/install/")}>{ui.install} →</a>
   </aside>
@@ -282,7 +294,7 @@ tobari policy allow --id <exact-pcy-id-from-policy-review>`,
 
   <section class="quickstart-complete">
     <h2>{ui.completionTitle}</h2>
-    <p>{ui.completion}</p>
+    <p>{completion}</p>
   </section>
 
   <section class="quickstart-next">
diff --git a/docs/architecture-site/src/content/docs/ja/index.mdx b/docs/architecture-site/src/content/docs/ja/index.mdx
--- a/docs/architecture-site/src/content/docs/ja/index.mdx
+++ b/docs/architecture-site/src/content/docs/ja/index.mdx
@@ -11,4 +11,8 @@ pagefind: true
 
 import HomeExperience from "../../../components/HomeExperience.astro";
 
-<HomeExperience />
+<HomeExperience
+  heading="コーディングエージェントを、選んだプロジェクトの中で動かす。"
+  lead="外部への HTTP/HTTPS 通信は、最初に止めます。内容を確認し、必要な通信だけを完全一致で許可してから、もう一度実行します。"
+  caveat="重要: 選んだプロジェクト内のファイルは、Tobari の保護対象ではありません。エージェントはその範囲を読み書きできます。"
+/>
diff --git a/docs/architecture-site/src/content/docs/ja/start/quickstart.mdx b/docs/architecture-site/src/content/docs/ja/start/quickstart.mdx
--- a/docs/architecture-site/src/content/docs/ja/start/quickstart.mdx
+++ b/docs/architecture-site/src/content/docs/ja/start/quickstart.mdx
@@ -6,4 +6,8 @@ description: 練習用プロジェクトで拒否を確認し、ホストで完
 
 import QuickstartWalkthrough from "../../../../components/QuickstartWalkthrough.astro";
 
-<QuickstartWalkthrough />
+<QuickstartWalkthrough
+  intro="練習用プロジェクトで、未許可の通信が一度止まり、ホストで確認して許可した後、新しいリクエストが外部へ進むところまで試します。"
+  prerequisite="先にインストールを完了し、対話型ターミナルを使ってください。選んだプロジェクト内のファイルは保護対象ではないため、実際の作業ディレクトリではなく練習用ディレクトリを使います。"
+  completion="Tobari が未許可の通信を止め、人がホストで内容を確認し、必要な完全一致の通信だけを許可した後に、新しいリクエストを送れることを体験しました。"
+/>
